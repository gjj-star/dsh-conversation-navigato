# dsh-conversation-navigator 安装示例
# 把下面这段追加到 <DSH_HOME>/profiles/<profile>/cordis.patch.yml 的顶层数组中
# (<DSH_HOME> 默认为 %USERPROFILE%/.dsh,Web 端 profile 通常为 web)。

- insert:
    - id: ui-conversation-navigator
      name: 'dsh-conversation-navigator'
